const newItem = () => {
    let task_text = document.getElementById('task_text').value;
    console.log(task_text);

    // copying creating, and appending new todo-task list item
    // const list_item = document.createElement('')
}